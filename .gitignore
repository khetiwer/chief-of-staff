# Personal runtime data — never publish
state/
sessions/
reports/

# Personal planning and context — not portfolio content
PLAN.md
network-accountability-agent-spec.md

# Secrets and credentials
*.env
.env.*
*.secret
*.key
*.pem

# Claude Code session data — exclude runtime/local-only files
.claude/settings.json
.claude/scheduled_tasks.lock

# Personal scratch / explainer artifacts — not part of the project
hooks-explainer.html
watchdog-explainer.html

# OneDrive artifacts
~$*
.~lock.*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Memory files (session-specific, not part of the brain)
memory/
